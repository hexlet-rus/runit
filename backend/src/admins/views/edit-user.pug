html
    head
        meta(charset='utf-8')
        meta(name='viewport' content='width=device-width, initial-scale=1')
        title Edit User
        link(href='	https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css' rel='stylesheet')  
    body
      nav.navbar.navbar-expand-lg
          .container-fluid
            .collapse.navbar-collapse#navbarSupportedContent
              ul.navbar-nav.me-auto.mb-2.mb-lg-0
                li.nav-item
                  a.nav-link(href='/api/admin/users') Назад
      .container-fluid
        h1.text-center.font-weight-bold.pb-3
                | Редактирование профиля
        .d-flex.align-items-center.justify-content-center
          form(action=`/api/admin/users/edit/${updateUserDto.id}`, method="post")
            div.mb-3
              label.form-label(for="username") Имя
              input.form-control(id="username" name="username" type="text" value=updateUserDto.username)
              if status === 'validationFailed' && errors.username
                .alert.alert-warning(role="alert") #{errors.username.message}
            div.mb-3
              label.form-label(for="email") Емейл
              input.form-control(id="email" name="email" type="email" value=updateUserDto.email)
              if status === 'validationFailed' && errors.email
                .alert.alert-warning(role="alert") #{errors.email.message}
            div.mb-3
              label.form-label(for="status") Роль
              select.form-select(id="status" name="isAdmin")
                option(value="off" selected=updateUserDto.isAdmin) Пользователь
                option(value="on" selected=updateUserDto.isAdmin) Администратор
              if status === 'success'
                .alert.alert-success(role="alert") Пользователь успешно обновлен
              if status === 'failed'
                .alert.alert-warning(role="alert") #{errors}
            button.btn.btn-primary(type="submit") Сохранить