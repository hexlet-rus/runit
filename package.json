{
  "name": "runit",
  "version": "1.0.0",
  "description": "[![On Push](https://github.com/hexlet-rus/runit/actions/workflows/push.yml/badge.svg?event=push)](https://github.com/hexlet-rus/runit/actions/workflows/push.yml)",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start:prod": "npm run start:prod --prefix backend",
    "preinstall": "npm ci --prefix frontend && npm ci --prefix backend",
    "test": "playwright test",
    "start-backend-development": "NODE_ENV=development PORT=5001 npm start --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hexlet-rus/runit.git"
  },
  "author": "",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/hexlet-rus/runit/issues"
  },
  "homepage": "https://github.com/hexlet-rus/runit#readme",
  "devDependencies": {
    "@playwright/test": "^1.30.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
