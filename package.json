{
  "name": "runit",
  "private": true,
  "version": "1.0.0",
  "description": "online editor for coding",
  "engines": {
    "node": ">=18.13.0"
  },
  "type": "module",
  "scripts": {
    "dev": "mkdir -p data && npm run db:setup && tsx watch src/server.ts",
    "build": "npm run db:setup && tsc",
    "start": "node dist/server.js",
    "db:setup": "npm run db:generate && npm run db:push",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio",
    "db:reset": "rm -rf data drizzle && npm run db:setup",
    "test": "playwright test",
    "test:ci": "npx start-server-and-test start-server http://localhost:3000 test",
    "test:withDocker": "WITHOUT_DOCKER = false playwright test",
    "test:debug": "yarn test -- --ui"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hexlet-rus/runit.git"
  },
  "author": "",
  "license": "Affero GPL 3.0",
  "bugs": {
    "url": "https://github.com/hexlet-rus/runit/issues"
  },
  "homepage": "https://github.com/hexlet-rus/runit#readme",
  "dependencies": {
    "@trpc/server": "^11.4.3",
    "better-sqlite3": "^12.2.0",
    "drizzle-orm": "^0.44.3",
    "fastify": "^5.4.0",
    "zod": "^4.0.10"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.9.0",
    "@playwright/test": "^1.54.1",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.1.0",
    "drizzle-kit": "^0.31.4",
    "jest": "^29.7.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
